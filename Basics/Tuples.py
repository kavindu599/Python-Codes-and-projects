# we can not change tuples

numbers = (1,2,3)
print(numbers)

# we can access the items
print(numbers[0])

# we can count the number of items in the tuple
number_count = numbers.count
print(number_count)

# we can get the index number related to the item
print(numbers.index(2))



